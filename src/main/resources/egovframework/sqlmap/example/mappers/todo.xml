<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="todo">
	
<select id="getTodo" parameterType="String" resultType="todoVo">
	SELECT *
	FROM todo
	where req_id=#{reqId}
</select>
<insert id="insertTodo">
	INSERT INTO todo (
    todo_id,
    todo_start,
    todo_end,
    todo_cont,
    todo_title,
    percent,
    todo_importance,
    todo_level,
    req_id,
    mem_id,
    todo_parentid,
    del
) VALUES (
    todo_seq.nextval,
    TO_DATE(#{todoStart},'YYYY/MM/DD'),
    TO_DATE(#{todoEnd},'YYYY/MM/DD'),
    #{todoCont},
    #{todoTitle},
    '0',
    #{todoImportance},
    NULL,
    #{reqId},
    #{memId},
    #{todoParentid},
    NULL                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      
)


</insert>

</mapper>