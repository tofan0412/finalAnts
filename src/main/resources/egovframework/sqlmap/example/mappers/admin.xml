<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ants.com.admin.mapper.AdminMapper">

	<select id="getAdmin" parameterType="ants.com.admin.model.AdminVo" resultType="ants.com.admin.model.AdminVo">
		SELECT  admin_id,
				admin_pass
		FROM ADMIN
		WHERE ADMIN_ID = #{adminId}
	</select>
	
	<select id="adlogincheck" parameterType="ants.com.admin.model.AdminVo" resultType="ants.com.admin.model.AdminVo">
		SELECT * FROM ADMIN
		WHERE ADMIN_ID = #{adminId}
		AND ADMIN_PASS = #{adminPass}
	</select>



	<!-- 회원 목록 -->
	<!-- 
	<select id="memberlist" parameterType="ants.com.member.model.MemberVo" resultType="ants.com.member.model.MemberVo">
		SELECT *
		FROM 	MEMBER
		ORDER BY mem_id desc
	</select>
	-->
	<!-- 멤버 등록 -->
	<!-- 
	<insert id="insertMember" parameterType="ants.com.member.model.MemberVo">
		INSERT INTO MEMBER 
			(mem_id, mem_name, mem_tel,mem_type)
		VALUES
			(${memId},${memName},${memTel},${memType})
	</insert>
	 -->
	<!-- 멤버 정보 상세보기 -->
	<!-- 
	<select id="viewMember" parameterType="ants.com.member.model.MemberVo" resultType="ants.com.member.model.MemberVo">
		SELECT
			mem_id as memId,
			mem_name as memName,
			mem_tel as memTel,
			mem_type as memType
		FROM MEMBER
		WHERE mem_id = #{memId}
	</select>
	 -->
</mapper>